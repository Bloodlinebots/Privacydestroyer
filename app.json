{
  "name": "SavexUserBot",
  "description": "Multi-session Telegram userbot with spam, privacy saver, and session logging.",
  "repository": "https://github.com/savexedits/multi-userbot",
  "env": {
    "API_ID": {
      "description": "Your Telegram API ID from my.telegram.org",
      "required": true
    },
    "API_HASH": {
      "description": "Your Telegram API Hash from my.telegram.org",
      "required": true
    },
    "BOT_TOKEN": {
      "description": "Your bot token from @BotFather",
      "required": true
    },
    "MONGO_URI": {
      "description": "MongoDB URI (e.g., from MongoDB Atlas)",
      "required": true
    }
  },
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ]
}
